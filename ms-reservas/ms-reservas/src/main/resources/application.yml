server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nombre_de_tu_bd?useSSL=false&serverTimezone=UTC
    username: root
    password: "" # En Laragon va vacío por defecto
  flyway:
    locations: classpath:db/migration

# Si es un archivo .yml
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/