server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sistema_vehiculos?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ""
  flyway:
    locations: classpath:db/migration

# Si es un archivo .yml
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/